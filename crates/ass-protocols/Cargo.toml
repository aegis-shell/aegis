[package]
name = "ass-protocols"
description = "Wayland protocol interface tables (xdg-shell), generated once and shared by backend and server"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
build = "build.rs"

[build-dependencies]
cc = "1"
