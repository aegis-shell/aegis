[package]
name = "aegis-tray"
description = "StatusNotifierItem system tray service for the aegis compositor: watcher/host on the session bus, shared by shell chrome components"
readme = "README.md"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
# SNI IconName resolution through the freedesktop icon theme: the same
# lookup (aegis-desktop-entries) and decode (image + rsvg-convert) path as
# the compositor's app icon cache.
aegis-desktop-entries = { workspace = true }
image = { workspace = true }
log = { workspace = true }
zbus = { workspace = true }

[lints]
workspace = true
