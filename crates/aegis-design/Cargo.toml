[package]
name = "aegis-design"
description = "Product design tokens, themes, and materials for ass chrome"
readme = "README.md"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
build = "build.rs"

[dependencies]
lens = { workspace = true }
# Direct dep so this crate's test harness receives DEP_LENS_RPATHS and can
# resolve the sibling Lens/Flux build trees through build.rs.
lens-sys = { workspace = true }
