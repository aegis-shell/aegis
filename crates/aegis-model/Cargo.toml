[package]
name = "aegis-model"
description = "Shared Aegis model: state, identities, geometry, and deterministic rules"
readme = "README.md"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
serde = { workspace = true, optional = true }

[features]
default = []
serde = ["dep:serde"]

[dev-dependencies]
serde_json.workspace = true
