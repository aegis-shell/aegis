[package]
name = "aegis-agent"
description = "Aegis agent runtime: self-contained agent driving the desktop through aegis-mcp (Persona: fuji / 宓姬)"
readme = "README.md"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[lib]
name = "aegis_agent"
path = "src/lib.rs"

[[bin]]
name = "aegis-agent"
path = "src/bin/aegis-agent.rs"


[dependencies]
# Agent side: providers, tools, MCP client, sessions. Never any aegis-* crate.
dirs = { workspace = true }
tokio = { version = "1", features = [
    "rt-multi-thread",
    "macros",
    "process",
    "io-util",
    "signal",
    "time",
] }
reqwest = { version = "0.13", default-features = false, features = [
    "rustls",
    "webpki-roots",
    "json",
    "stream",
    "gzip",
    "brotli",
] }
futures = "0.3"
rustyline = "15"
glob = "0.3"
regex = "1"
base64 = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
toml = { workspace = true }
thiserror = { workspace = true }
clap = { version = "4", features = ["derive"] }

[dev-dependencies]
tempfile = "3"
