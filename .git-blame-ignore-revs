# .git-blame-ignore-revs
# List of commit SHAs to ignore in git blame (e.g. bulk formatting, mass migrations).
# Configure git locally to use this file:
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
